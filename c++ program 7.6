#include <sstream>
#include <vector>
#include <iostream>
using namespace std;

// Complete this function
vector<int> parseInts(string str) {
    vector<int> result;
    stringstream ss(str);
    int number;
    char comma;

    // Read number, then comma, in a loop
    while (ss >> number) {
        result.push_back(number);
        ss >> comma; // discard comma
    }

    return result;
}

int main() {
    string str;
    cin >> str;  // e.g., "23,4,56"
    vector<int> integers = parseInts(str);
    for (int i = 0; i < integers.size(); i++) {
        cout << integers[i] << "\n";
    }
    return 0;
}
